use std::collections::HashMap;

use crate::model::{PlayerColor, SyntaxStyle};

pub fn set_light_syntax_colors() -> HashMap<&'static str, SyntaxStyle> {
    let mut syntaxes = HashMap::new();

    syntaxes
}

pub fn set_light_player_colors() -> Vec<PlayerColor> {
    let mut players = Vec::new();

    players
}
